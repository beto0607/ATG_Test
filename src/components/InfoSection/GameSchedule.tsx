import React, { useState } from "react";
const GameSchedule: React.FC = ({ title }) => {
    return (
        <div>
            <h4>{title}</h4>
        </div>
    );
};

export default GameSchedule;
