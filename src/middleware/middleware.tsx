import * as constants from "../constants/action-types";
// const URL_BASE = 'localhost:3001';

// export function forbiddenWordsMiddleware({ dispatch: any }) {
//     return function (next: any) {
//         return function (action: any) {
//             // do your stuff
//             if (action.type === constants.SEARCH_TEXT) {
//                 if (action.text.length) {

//                     return dispatch({ type: constants.GET_GAME_SCHEDULE});
//                 }
//             }
//             return next(action);
//         };
//     };
// }